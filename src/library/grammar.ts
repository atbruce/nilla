// @ts-nocheck
const articles = ["a", "an", "the"]

const conjunctions = ["and", "but", "for", "nor", "or", "so", "yet"]

const prepositions = ["at", "by", "in", "of", "on", "to"]

export const GRAMMAR = {articles, conjunctions, prepositions}